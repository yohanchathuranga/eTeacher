import { CheckForumTypeDirective } from './check-forum-type.directive';

describe('CheckForumTypeDirective', () => {
  it('should create an instance', () => {
    const directive = new CheckForumTypeDirective();
    expect(directive).toBeTruthy();
  });
});
